# Optional. Local full URL, or omit to use `/api` (Vite proxy in dev; rewrite on Vercel).
# Always include `/api`. Never set this to your Vercel URL alone — use your API host instead.
VITE_API_URL=http://localhost:4000/api
