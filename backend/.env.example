PORT=4000
MONGODB_URI=
JWT_SECRET=
JWT_EXPIRES_IN=7d
NODE_ENV=development
# Production: your deployed frontend origin (scheme + host, no trailing path)
FRONTEND_URL=http://localhost:3000
# Optional: comma-separated extra origins (Vercel preview URLs, alternate domains)
# ALLOWED_ORIGINS=https://preview-abc.vercel.app
